data segment
    a dw 1001h
    b db 04h
    rev dw ?
    ispalindrome db 0h
data ends

code segment
assume cs:code,ds:data

start: mov ax,data
       mov ds,ax
       
       mov ax,a
       mov cl,b
       
       rol al,cl
       rol ah,cl
       mov bh,ah
       mov ah,al
       mov al,bh
       mov rev,ax
       
       cmp ax,a
       jnz end
       mov ispalindrome,01h

end:       
mov ah,4ch
int 21h
code ends
end start